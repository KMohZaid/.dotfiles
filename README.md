# Dotfiles

# NOTE : for now, store this at ~/.dotfiles because nvim directory is symlinked to ~/.config/nvim based on ~/.dotfiles path. IDK why nix not copying nvim folder
# ALSO : for this symlink, passed variable to home manager with hardcoded flake dir to "waifu" user (/home/waifu/.dotfiles/), gotta run with --impure
